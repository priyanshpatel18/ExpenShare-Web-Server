<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OTP Verification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
      }

      .container {
        text-align: center;
        border-radius: 8px;
        padding: 20px;
        width: 300px;
      }

      h1 {
        color: #333333;
        font-size: 24px;
        margin-bottom: 20px;
      }

      .otp-container {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
      }

      .otp-box {
        font-size: 25px;
        background-color: #ffffff;
        border-radius: 6px;
        padding: 20px;
        margin: 0 5px;
        border: 1px solid #cccccc;
        width: 40px;
        height: 40px;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      p {
        color: #666666;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>OTP Verification</h1>
      <div class="otp-container">
        <% otp.split('').forEach(char => { %>
        <div class="otp-box"><%= char %></div>
        <% }); %>
      </div>
      <p>Please use the OTP above to complete the verification process.</p>
    </div>
  </body>
</html>
